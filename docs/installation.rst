============
Installation
============

At the command line::

    $ easy_install hypernova-django

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv hypernova-django
    $ pip install hypernova-django
